# GENERATED BY wallust

max-history=100

font=SF Pro 10
width=350
height=100
border-radius=5

default-timeout=5000
ignore-timeout=1

on-button-left=invoke-default-action
on-button-right=dismiss

background-color={{background}}
text-color={{foreground}}
border-color={{background | blend(foreground)}}
